<table{{ attributes.addClass(classes) }}>
  <tr>
    <th>Name</th>
    <th>Downloads</th>
  </tr>
  {% for stat in stats %}
    <tr>
      <td>{{ link(stat.title, stat.url, {'target': target}) }}</td>
      <td>{{ stat.downloads }}</td>
    </tr>
  {% endfor %}
</table>
